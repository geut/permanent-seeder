#!/usr/bin/env node

const { SDK } = require('@geut/permanent-seeder-sdk')

const seederRepl = async () => {
  const sdk = new SDK()
  await sdk.connect()
  sdk.repl()
}

seederRepl().catch(error => {
  console.error(error)
  process.exit(1)
})
